*.sublime-project
*.sublime-workspace

/data

# Dependencies and build output
node_modules/
client/build/

# Claude Code local files. CLAUDE.md itself is TRACKED (issue #272): it is
# shared project documentation, and while it was ignored its corrections lived
# on one machine and were invisible to everyone else.
.claude/


# Git worktrees (superpowers:using-git-worktrees scratch space)
.worktrees/
worktrees/
.superpowers/

# Calculation-audit fixtures: evidence for one run, not source.
tools/audit/fixtures/
